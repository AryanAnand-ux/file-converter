# Contributing Guide

We welcome contributions! Please follow these rules:

1. **TypeScript Typing**: Avoid using `any`. Use `unknown` or strictly defined types wherever possible.
2. **ESLint**: Ensure `npm run lint` passes with zero errors before submitting pull requests.
3. **Styling**: Use Tailwind utility classes in a consistent manner. Maintain high-quality visual polish.