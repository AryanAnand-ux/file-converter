# Deployment

ConvertIO can be deployed to any static host (Vercel, Netlify, GitHub Pages) since it compiles to a pure client-side bundle.

To deploy on Vercel:
```bash
npm run build
vercel --prod
```