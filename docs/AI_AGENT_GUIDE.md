# AI Agent Guide

Guidelines for future AI agents:
1. Always maintain the client-side design paradigm. Do not add server endpoints or database configurations.
2. Ensure you clean up canvas contexts and disconnect observers to avoid memory leaks.
3. Fix linting errors immediately.